# Build-Output (CMake/Ninja) — pro Maschine unterschiedlich, wird immer neu generiert
build/

# clangd-Index-Cache
.cache/

# Editor-Konfiguration (maschinenspezifisch, z.B. absolute Pfade)
.vscode/

# Symlink auf die Compile-Datenbank im Projektroot (falls angelegt)
/compile_commands.json

# vcpkg-Installationsordner (falls er je im Projektroot landet)
vcpkg_installed/

# Lokale Claude-Code-Einstellungen (Berechtigungen dieser Maschine)
.claude/settings.local.json

# Windows-Dateisystem-Müll
Thumbs.db
desktop.ini

# Persönliche Arbeitsdateien, nicht fürs Repo bestimmt
to-do.md
Notizen.md
CLAUDE.md